package dev.duma.android.hal.plugins.sunmi.tms.handler

// Removed: success(), error(), unsupportedMethod() helpers
// Use CommandResult.Success(), CommandResult.Failure(), CommandResult.unsupportedMethod() directly
